# Phase 8: Build + Package

## Priority: Low
## Status: Pending

## Steps
1. Configure `tauri.conf.json` for production build
2. Set app name, version, icon
3. Run `npm run tauri build`
4. Test .msi installer on Windows
5. Add to README: install instructions

## Success Criteria
- `npm run tauri build` produces working .msi
- Install and run without dev dependencies
